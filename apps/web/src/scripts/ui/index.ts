// Behavior shared by every page.
import { setupLanguageSwitch } from "./lang-switch";
import { setupReveal } from "./reveal";
import { setupThemeToggle } from "./theme";

setupLanguageSwitch();
setupThemeToggle();
setupReveal();
