{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types", "@cloudflare/vitest-plugin/types"],
    // Tests index into fixtures they have just built: a failed lookup fails the test anyway.
    "noUncheckedIndexedAccess": false
  },
  "include": ["./**/*.ts", "../src/**/*.ts"]
}
