PostTheDoc - {{ intro }}
{% for role, calls in groups.items() %}

== {{ role }} ({{ calls | length }}) ==
{% for c in calls %}

- {{ c.title }}
  {{ c.institution_name }}{% if c.region %} ({{ regions[c.region] }}){% endif %}

  {{ t.sector }}: {{ c.ssd | join(", ") if c.ssd else (c.gsd | join(", ") if c.gsd else t.sector_unspecified) }}
  {{ t.deadline }}: {{ c.deadline.strftime("%d/%m/%Y %H:%M") if c.deadline else t.not_available }}
  {{ c.url }}
{% endfor %}
{% endfor %}

--
{{ t.manage }}: {{ manage_url }}
{{ t.unsubscribe }}: {{ unsubscribe_url }}
{{ t.privacy }}: {{ privacy_url }}
{{ t.support }}: {{ support_url }}
